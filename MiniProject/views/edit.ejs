<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Mini Project</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
  <div class="w-full min-h-screen bg-zinc-900 text-white p-3">

    <h5 class="m-2 mt-5">You can Edit your post</h5>

    <form action="/update/<%= post._id%>" method="POST" class="mt-5">
      <textarea name="content" placeholder="What's on your mind?"
  class="block w-full sm:w-1/2 p-3 outline-none resize-none bg-transparent border-2 border-zinc-800 rounded-md text-white"
  required><%= post.content %></textarea>

      <button type="submit" class="px-4 py-2 w-40 bg-yellow-500 hover:bg-yellow-600 text-zinc-800 rounded-md block mt-3">
        Update Post
      </button>
    </form>

  </div>
</body>

</html>
